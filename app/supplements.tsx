"use client";

import { useEffect, useMemo, useState } from "react";
import { supabase } from "../lib/supabase";
import { formatScheduleDate,isSupplementDue,supplementScheduleSummary,type SupplementFrequency,type SupplementSchedule } from "../lib/supplement-schedule";

type Supplement=SupplementSchedule&{id:string;name:string;brand:string|null;dose_value:number;dose_unit:string;concentration_mg_per_ml:number|null;composition_notes:string|null;scheduled_time:string;active:boolean;notes:string|null;reminder_enabled:boolean;follow_up_minutes:number|null};
type Log={supplement_id:string;scheduled_on:string;status:"taken"|"skipped";actual_dose_value?:number|null;actual_dose_unit?:string|null;preset_dose_value?:number|null;preset_dose_unit?:string|null;concentration_mg_per_ml?:number|null};
export type SupplementSummary={total:number;recorded:number;next:string|null};
const localDate=(date=new Date())=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}-${String(date.getDate()).padStart(2,"0")}`;
const today=()=>localDate(),days=[{value:1,short:"M",label:"Monday"},{value:2,short:"T",label:"Tuesday"},{value:3,short:"W",label:"Wednesday"},{value:4,short:"T",label:"Thursday"},{value:5,short:"F",label:"Friday"},{value:6,short:"S",label:"Saturday"},{value:0,short:"S",label:"Sunday"}];
const supplementSelect="id,name,brand,dose_value,dose_unit,concentration_mg_per_ml,composition_notes,scheduled_time,scheduled_weekdays,schedule_frequency,schedule_anchor_date,schedule_interval_days,active,notes,reminder_enabled,follow_up_minutes";

export async function loadSupplementSummary(userId:string):Promise<SupplementSummary>{
  const [{data:supplements},{data:logs}]=await Promise.all([supabase.from("supplements").select(supplementSelect).eq("user_id",userId).eq("active",true).order("scheduled_time"),supabase.from("supplement_logs").select("supplement_id").eq("user_id",userId).eq("scheduled_on",today())]);
  const todaysItems=((supplements||[]) as Supplement[]).filter(item=>isSupplementDue(item,new Date())),done=new Set((logs||[]).map(log=>log.supplement_id)),next=todaysItems.find(item=>!done.has(item.id));
  return {total:todaysItems.length,recorded:todaysItems.filter(item=>done.has(item.id)).length,next:next?`${next.name} · ${String(next.scheduled_time).slice(0,5)}`:null};
}

export function SupplementsArea({userId,advanced,onSummary,onNotice}:{userId:string;advanced:boolean;onSummary:(summary:SupplementSummary)=>void;onNotice:(message:string)=>void}){
  const [items,setItems]=useState<Supplement[]>([]),[logs,setLogs]=useState<Log[]>([]),[weekLogs,setWeekLogs]=useState<Log[]>([]),[editing,setEditing]=useState<Supplement|"new"|null>(null),[dosing,setDosing]=useState<Supplement|null>(null),[busy,setBusy]=useState(false);
  const [calendarMonth,setCalendarMonth]=useState(()=>new Date(new Date().getFullYear(),new Date().getMonth(),1)),[calendarLogs,setCalendarLogs]=useState<Log[]>([]),[selectedDate,setSelectedDate]=useState(today());
  async function load(){const start=new Date();start.setDate(start.getDate()-6);const [{data:supplements,error},{data:records},{data:history}]=await Promise.all([supabase.from("supplements").select(supplementSelect).eq("user_id",userId).order("scheduled_time"),supabase.from("supplement_logs").select("supplement_id,status,scheduled_on,actual_dose_value,actual_dose_unit,preset_dose_value,preset_dose_unit,concentration_mg_per_ml").eq("user_id",userId).eq("scheduled_on",today()),supabase.from("supplement_logs").select("supplement_id,status,scheduled_on,actual_dose_value,actual_dose_unit,preset_dose_value,preset_dose_unit").eq("user_id",userId).gte("scheduled_on",localDate(start))]);if(error){onNotice(error.message);return}setItems((supplements||[]) as Supplement[]);setLogs((records||[]) as Log[]);setWeekLogs((history||[]) as Log[]);onSummary(await loadSupplementSummary(userId))}
  async function loadCalendar(month=calendarMonth){const first=localDate(new Date(month.getFullYear(),month.getMonth(),1)),last=localDate(new Date(month.getFullYear(),month.getMonth()+1,0));const {data,error}=await supabase.from("supplement_logs").select("supplement_id,status,scheduled_on,actual_dose_value,actual_dose_unit,preset_dose_value,preset_dose_unit").eq("user_id",userId).gte("scheduled_on",first).lte("scheduled_on",last).order("scheduled_on");if(error)onNotice(error.message);else setCalendarLogs((data||[]) as Log[])}
  useEffect(()=>{load()},[userId]);useEffect(()=>{loadCalendar(calendarMonth)},[userId,calendarMonth]);
  async function save(event:React.FormEvent<HTMLFormElement>){
    event.preventDefault();setBusy(true);
    const form=new FormData(event.currentTarget),unit=String(form.get("unit")),frequency=String(form.get("frequency")) as SupplementFrequency,existing=editing!=="new"?editing:null;
    const values={user_id:userId,name:String(form.get("name")),brand:String(form.get("brand")||"")||null,dose_value:Number(form.get("dose")),dose_unit:unit,concentration_mg_per_ml:advanced?(unit==="ml"&&form.get("concentration")?Number(form.get("concentration")):null):existing?.concentration_mg_per_ml??null,composition_notes:advanced?String(form.get("composition")||"")||null:existing?.composition_notes??null,scheduled_time:String(form.get("time")),scheduled_weekdays:frequency==="weekly"?form.getAll("weekdays").map(Number):existing?.scheduled_weekdays||[0,1,2,3,4,5,6],schedule_frequency:frequency,schedule_anchor_date:frequency==="weekly"?null:String(form.get("anchorDate")),schedule_interval_days:frequency==="custom"?Number(form.get("intervalDays")):null,notes:String(form.get("notes")||"")||null,reminder_enabled:form.get("reminder")==="on",follow_up_minutes:form.get("followUp")?Number(form.get("followUp")):null,updated_at:new Date().toISOString()};
    const result=editing!=="new"&&editing?await supabase.from("supplements").update(values).eq("id",editing.id).eq("user_id",userId):await supabase.from("supplements").insert(values);setBusy(false);if(result.error){onNotice(result.error.message);return}const created=editing==="new";setEditing(null);onNotice(created?"Supplement schedule securely saved":"Supplement schedule updated");load()
  }
  async function record(item:Supplement,status:Log["status"],actualDose=item.dose_value){const existing=logs.find(log=>log.supplement_id===item.id),actual={status,actual_dose_value:status==="taken"?actualDose:null,actual_dose_unit:status==="taken"?item.dose_unit:null,recorded_at:new Date().toISOString()},snapshot={preset_dose_value:item.dose_value,preset_dose_unit:item.dose_unit,concentration_mg_per_ml:item.concentration_mg_per_ml,composition_snapshot:item.composition_notes};const result=existing?await supabase.from("supplement_logs").update(actual).eq("user_id",userId).eq("supplement_id",item.id).eq("scheduled_on",today()):await supabase.from("supplement_logs").insert({user_id:userId,supplement_id:item.id,scheduled_on:today(),...actual,...snapshot});if(result.error)onNotice(result.error.message);else{setDosing(null);onNotice(status==="taken"?`${item.name} · ${actualDose} ${item.dose_unit} recorded`:`${item.name} marked skipped`);await load();await loadCalendar()}}
  async function toggle(item:Supplement){const {error}=await supabase.from("supplements").update({active:!item.active,updated_at:new Date().toISOString()}).eq("id",item.id).eq("user_id",userId);if(error)onNotice(error.message);else load()}
  const active=items.filter(item=>item.active),dueToday=active.filter(item=>isSupplementDue(item,new Date())),recorded=dueToday.filter(item=>logs.some(log=>log.supplement_id===item.id)).length,expected=Array.from({length:7},(_,offset)=>{const date=new Date();date.setDate(date.getDate()-offset);return active.filter(item=>isSupplementDue(item,date)).length}).reduce((total,count)=>total+count,0),taken=weekLogs.filter(log=>log.status==="taken"&&active.some(item=>item.id===log.supplement_id)).length,skipped=weekLogs.filter(log=>log.status==="skipped"&&active.some(item=>item.id===log.supplement_id)).length,adherence=expected?Math.round(taken/expected*100):0;
  return <div className="content-stack supps-area"><section className="section-head"><div><p className="eyebrow">PERSONAL RECORDS · V0.14C</p><h1>Supplements.</h1></div><button className="primary" onClick={()=>setEditing("new")}>＋ Add</button></section><section className="supp-summary"><div><strong>{recorded}</strong><span>of {dueToday.length}</span></div><p>scheduled today</p></section>{active.length>0&&<section className="supp-adherence"><div><p className="eyebrow">LAST 7 DAYS</p><strong>{adherence}%</strong><span>taken as scheduled</span></div><p><b>{taken}</b> taken · <b>{skipped}</b> skipped · <b>{Math.max(0,expected-taken-skipped)}</b> unrecorded</p><small>Unrecorded means no Taken or Skip choice was saved. It is not a medical assessment.</small></section>}<div className="medical-boundary"><strong>Logging only</strong><p>Physique records the details you enter. It does not recommend products or dosages and does not provide medical advice.</p></div>{items.length===0?<section className="empty-training"><span>◉</span><h2>Your list is empty.</h2><p>Add supplements you already use to create a personal schedule.</p></section>:<section className="supp-stack">{items.map(item=>{const log=logs.find(entry=>entry.supplement_id===item.id);return <article className={`supp-card ${!item.active?"paused":""}`} key={item.id}><header><div className="supp-time">{item.scheduled_time.slice(0,5)}</div><div><h2>{item.name}</h2><p>{item.dose_value} {item.dose_unit}{item.brand?` · ${item.brand}`:""}</p>{advanced&&item.dose_unit==="ml"&&item.concentration_mg_per_ml&&<small className="supp-concentration">{item.concentration_mg_per_ml} mg/ml · {(item.dose_value*item.concentration_mg_per_ml).toFixed(1)} mg total</small>}</div><button onClick={()=>setEditing(item)}>Edit</button></header><div className="supp-reminder-state"><span>{item.reminder_enabled?"◉":"○"}</span><small>{item.reminder_enabled?`Reminder at ${item.scheduled_time.slice(0,5)}${item.follow_up_minutes?` + follow-up after ${item.follow_up_minutes} min`:""}`:"Reminders off for this item"}</small></div><small className="supp-days">{supplementScheduleSummary(item)}</small>{advanced&&item.composition_notes&&<p className="supp-composition"><b>Composition</b>{item.composition_notes}</p>}{log?.status==="taken"&&<p className="supp-recorded-dose">Recorded today · <b>{log.actual_dose_value??log.preset_dose_value??item.dose_value} {log.actual_dose_unit??log.preset_dose_unit??item.dose_unit}</b>{log.preset_dose_value&&Number(log.actual_dose_value)!==Number(log.preset_dose_value)?` · preset ${log.preset_dose_value} ${log.preset_dose_unit}`:""}</p>}{item.notes&&<p className="supp-notes">{item.notes}</p>}<div className="supp-card-controls"><button onClick={()=>toggle(item)}>{item.active?"Pause":"Resume"}</button>{item.active&&<><button className={log?.status==="skipped"?"selected":""} onClick={()=>record(item,"skipped")}>Skip</button>{advanced&&<button onClick={()=>setDosing(item)}>Adjust dose</button>}<button className={log?.status==="taken"?"taken selected":"taken"} onClick={()=>record(item,"taken")}>✓ Taken</button></>}</div></article>})}</section>}<SupplementHistory month={calendarMonth} logs={calendarLogs} items={items} selectedDate={selectedDate} onSelect={setSelectedDate} onMonth={setCalendarMonth}/>{editing&&<SupplementEditor item={editing==="new"?null:editing} advanced={advanced} busy={busy} onClose={()=>setEditing(null)} onSave={save}/>} {dosing&&<DoseRecorder item={dosing} current={logs.find(log=>log.supplement_id===dosing.id)} busy={busy} onClose={()=>setDosing(null)} onRecord={dose=>record(dosing,"taken",dose)}/>}</div>;
}

function SupplementEditor({item,advanced,busy,onClose,onSave}:{item:Supplement|null;advanced:boolean;busy:boolean;onClose:()=>void;onSave:(event:React.FormEvent<HTMLFormElement>)=>void}){
  const [reminder,setReminder]=useState(item?.reminder_enabled??true),[selectedDays,setSelectedDays]=useState<number[]>(item?.scheduled_weekdays??[0,1,2,3,4,5,6]),[unit,setUnit]=useState(item?.dose_unit||"mg"),[frequency,setFrequency]=useState<SupplementFrequency>(item?.schedule_frequency||"weekly"),[intervalDays,setIntervalDays]=useState(item?.schedule_interval_days||10),[anchorDate,setAnchorDate]=useState(item?.schedule_anchor_date||today());
  const nextCustomDate=useMemo(()=>{const date=new Date(`${anchorDate}T12:00:00`);date.setDate(date.getDate()+intervalDays);return localDate(date)},[anchorDate,intervalDays]);
  return <div className="mini-picker supp-dialog"><form onSubmit={onSave}><header><div><p className="eyebrow">{item?"EDIT PERSONAL RECORD":"NEW PERSONAL RECORD"}</p><h2>{item?"Update supplement":"Add supplement"}</h2></div><button type="button" onClick={onClose}>×</button></header><label>Name<input name="name" maxLength={120} defaultValue={item?.name} required/></label><label>Brand <span>optional</span><input name="brand" maxLength={120} defaultValue={item?.brand||""}/></label><div className="dose-fields"><label>Dose<input name="dose" type="number" min="0.001" step="any" inputMode="decimal" defaultValue={item?.dose_value} required/></label><label>Unit<select name="unit" value={unit} onChange={event=>setUnit(event.target.value)}>{["mg","g","mcg","ml","units","capsule","capsules","tablet","tablets","scoop","scoops"].map(value=><option key={value}>{value}</option>)}</select></label></div>{advanced&&unit==="ml"&&<label>Concentration <span>mg/ml</span><input name="concentration" type="number" min="0.001" step="any" inputMode="decimal" defaultValue={item?.concentration_mg_per_ml??""} placeholder="Optional"/></label>}{advanced&&<label>Composition <span>optional</span><textarea name="composition" rows={3} maxLength={1500} defaultValue={item?.composition_notes||""} placeholder="Record ingredients or formulation details"/></label>}<label>Schedule<select name="frequency" value={frequency} onChange={event=>setFrequency(event.target.value as SupplementFrequency)}><option value="weekly">Selected days each week</option><option value="fortnightly">Every 2 weeks</option><option value="monthly">Every month</option><option value="custom">Custom number of days</option></select></label>{frequency==="weekly"?<fieldset className="supp-weekdays"><legend>Days taken</legend><div>{days.map(day=><label key={day.value} title={day.label}><input name="weekdays" type="checkbox" value={day.value} checked={selectedDays.includes(day.value)} onChange={event=>setSelectedDays(current=>event.target.checked?[...current,day.value]:current.filter(value=>value!==day.value))}/><span>{day.short}</span></label>)}</div><small>{selectedDays.length?supplementScheduleSummary({schedule_frequency:"weekly",schedule_anchor_date:null,schedule_interval_days:null,scheduled_weekdays:selectedDays}):"Choose at least one day"}</small></fieldset>:frequency==="custom"?<div className="custom-interval-fields"><label>Repeat every <span>days</span><input name="intervalDays" type="number" min="1" max="3650" step="1" inputMode="numeric" value={intervalDays} onChange={event=>setIntervalDays(Math.max(1,Number(event.target.value)))} required/></label><label>Count from<input name="anchorDate" type="date" value={anchorDate} onChange={event=>setAnchorDate(event.target.value)} required/><small>Previous records stay unchanged. Next scheduled dose: <b>{formatScheduleDate(nextCustomDate)}</b>.</small></label></div>:<label>{frequency==="fortnightly"?"First/next dose date":"First monthly dose date"}<input name="anchorDate" type="date" value={anchorDate} onChange={event=>setAnchorDate(event.target.value)} required/><small>{frequency==="fortnightly"?"Repeats every 14 days from this date.":"Repeats on this day each month; shorter months use their final day."}</small></label>}<label>Scheduled time<input name="time" type="time" defaultValue={item?.scheduled_time.slice(0,5)} required/></label><label className="supp-reminder-toggle"><span><strong>Remind me</strong><small>Requires separate device notification opt-in</small></span><input name="reminder" type="checkbox" checked={reminder} onChange={event=>setReminder(event.target.checked)}/></label>{reminder&&<label>Follow-up if still unrecorded <span>optional</span><select name="followUp" defaultValue={item?.follow_up_minutes||""}><option value="">No follow-up</option><option value="15">After 15 minutes</option><option value="30">After 30 minutes</option><option value="60">After 1 hour</option><option value="90">After 90 minutes</option><option value="120">After 2 hours</option></select></label>}<label>Personal notes <span>optional</span><textarea name="notes" rows={3} maxLength={1000} defaultValue={item?.notes||""}/></label><button className="primary" disabled={busy||(frequency==="weekly"&&selectedDays.length===0)}>{busy?"Saving…":item?"Save changes":"Save personal record"}</button></form></div>
}

function SupplementHistory({month,logs,items,selectedDate,onSelect,onMonth}:{month:Date;logs:Log[];items:Supplement[];selectedDate:string;onSelect:(date:string)=>void;onMonth:(month:Date)=>void}){const cells=useMemo(()=>{const first=new Date(month.getFullYear(),month.getMonth(),1),offset=(first.getDay()+6)%7,count=new Date(month.getFullYear(),month.getMonth()+1,0).getDate();return [...Array(offset).fill(null),...Array.from({length:count},(_,index)=>new Date(month.getFullYear(),month.getMonth(),index+1))]},[month]),selected=logs.filter(log=>log.scheduled_on===selectedDate),move=(delta:number)=>{const next=new Date(month.getFullYear(),month.getMonth()+delta,1);onMonth(next);onSelect(localDate(next))};return <section className="supp-history"><header><div><p className="eyebrow">HISTORY CALENDAR</p><h2>{month.toLocaleDateString("en-AU",{month:"long",year:"numeric"})}</h2></div><div><button onClick={()=>move(-1)} aria-label="Previous month">‹</button><button onClick={()=>move(1)} aria-label="Next month">›</button></div></header><div className="supp-calendar-labels">{["M","T","W","T","F","S","S"].map((day,index)=><span key={`${day}-${index}`}>{day}</span>)}</div><div className="supp-calendar">{cells.map((date,index)=>date?<button key={localDate(date)} className={`${localDate(date)===selectedDate?"selected":""} ${logs.some(log=>log.scheduled_on===localDate(date))?"recorded":""}`} onClick={()=>onSelect(localDate(date))} aria-label={`View ${formatScheduleDate(localDate(date))}`}><span>{date.getDate()}</span>{logs.some(log=>log.scheduled_on===localDate(date))&&<i/>}</button>:<span key={`blank-${index}`}/>)}</div><div className="supp-day-history"><strong>{formatScheduleDate(selectedDate)}</strong>{selected.length?selected.map(log=>{const item=items.find(value=>value.id===log.supplement_id),dose=log.actual_dose_value??log.preset_dose_value,unit=log.actual_dose_unit??log.preset_dose_unit;return <article key={`${log.supplement_id}-${log.scheduled_on}`}><span className={log.status}>{log.status==="taken"?"✓":"—"}</span><div><b>{item?.name||"Supplement"}</b><small>{log.status==="taken"&&dose?`${dose} ${unit} taken`:"Skipped"}</small></div></article>}):<p>No supplement records saved for this day.</p>}</div></section>}

function DoseRecorder({item,current,busy,onClose,onRecord}:{item:Supplement;current?:Log;busy:boolean;onClose:()=>void;onRecord:(dose:number)=>void}){const [dose,setDose]=useState(Number(current?.actual_dose_value??item.dose_value)),activeMg=item.dose_unit==="ml"&&item.concentration_mg_per_ml?dose*item.concentration_mg_per_ml:null;return <div className="mini-picker dose-dialog"><form onSubmit={event=>{event.preventDefault();onRecord(dose)}}><header><div><p className="eyebrow">ACTUAL DAILY DOSE</p><h2>{item.name}</h2></div><button type="button" onClick={onClose}>×</button></header><div className="preset-dose"><span>Preset</span><strong>{item.dose_value} {item.dose_unit}</strong><small>This saved schedule will not be changed.</small></div><label>Dose taken <span>{item.dose_unit}</span><input type="number" min="0.001" step="any" inputMode="decimal" value={dose} onChange={event=>setDose(Number(event.target.value))} required/></label>{activeMg!==null&&<p className="dose-conversion">At {item.concentration_mg_per_ml} mg/ml, this records <b>{activeMg.toFixed(1)} mg</b>.</p>}<p className="history-protection">This entry is stored as a dated snapshot. Future changes to the preset will not alter it.</p><button className="primary" disabled={busy||!dose}>{busy?"Saving…":"Record actual dose"}</button></form></div>}
